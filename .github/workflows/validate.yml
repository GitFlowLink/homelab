name: Validate

on:
  push:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      
      - name: Install yamllint
        run: pip install yamllint

      - name: Validate YAML
        run: yamllint ansible/ docker/
